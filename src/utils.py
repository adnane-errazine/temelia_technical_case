# from dataclasses import dataclass, field
# from typing import Dict, Any

# @dataclass
# class ChunkPayload:
#     chunk_text: str = ""
#     filename: str = ""
#     page: int = -1
#     token_count: int = 0
#     metadata: Dict[str, Any] = field(default_factory=dict) # field(default_factory=dict) for mutable defaults like dictionaries